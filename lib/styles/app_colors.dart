import 'package:flutter/material.dart';

class AppColors {
  static const white = (Color(0xffffffff));
  static const black = (Color(0xff000000));
  static const primary = (Color(0xffEA8C00));
}
