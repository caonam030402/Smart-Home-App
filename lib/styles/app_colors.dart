import 'package:flutter/material.dart';

class AppColors {
  static const white = (Color(0xffffffff));
  static const black = (Color(0xff000000));
  static const primary = (Color.fromARGB(255, 45, 130, 249));
  static const greyLight = (Color(0xffC9C7C6));
}
